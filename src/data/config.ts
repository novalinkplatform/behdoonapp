// این قالب برای فروش به خریدارهای مختلف است، پس هیچ دامنه‌ی واقعی نباید این‌جا هاردکد باشد —
// پیش‌فرض همیشه نسبی («/api/...» از همان دامنه) است. در بیلد خوداستقرار (Docker) همین پیش‌فرض
// به‌طور طبیعی درست است (Caddy روی همان دامنه پراکسی می‌کند). برای دیپلوی ابری خودمان (behbarapp.ir)
// که API روی زیردامنه‌ی جداست، VITE_API_BASE_URL صراحتاً موقع build ست می‌شود — نگاه کنید:
// package.json > build:demo. خریداری که خودش مستقیم روی Cloudflare دیپلوی می‌کند هم باید همین متغیر
// را به دامنه‌ی API خودش ست کند؛ بدون آن، به‌جای هاردکدشدن دامنه‌ی ما، به‌درستی نسبی می‌ماند.
export const API_BASE_URL = import.meta.env.VITE_API_BASE_URL ?? '';
