{
  "name": "behdoon",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build && node scripts/inject-site-origin.mjs",
    "build:demo": "cross-env SITE_ORIGIN=https://behbarapp.ir VITE_API_BASE_URL=https://api.behbarapp.ir VITE_DEMO_SALE_URL=https://behbarapp.ir/sale VITE_DEMO_PROMO_URL=https://zhaket.com npm run build",
    "preview": "vite preview",
    "deploy": "npm run build && wrangler pages deploy"
  },
  "devDependencies": {
    "@types/leaflet": "^1.9.22",
    "cross-env": "^10.1.0",
    "playwright": "^1.62.1",
    "typescript": "~6.0.2",
    "vite": "^8.2.0"
  },
  "dependencies": {
    "@fontsource/vazirmatn": "^5.3.0",
    "leaflet": "^1.9.4"
  }
}